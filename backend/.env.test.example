# Test Environment Variables
# Copy this file to .env.test for running tests
# This file is typically not needed as test settings have safe defaults
# Only override if you need custom test configuration

# Django Secret Key for tests (has a safe default in test settings)
# SECRET_KEY=test-secret-key

# Database URL (optional - test settings use in-memory SQLite by default)
# DATABASE_URL=sqlite:///:memory:

# Log level for tests (optional)
# LOG_LEVEL=WARNING
