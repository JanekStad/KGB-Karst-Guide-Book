# Production Environment Variables
# Copy this file to .env.prod as a reference
# DO NOT commit actual production secrets to version control!
# In Railway or other platforms, set these as environment variables in the platform's UI

# Django Secret Key - REQUIRED - Generate a strong random secret key
# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-production-secret-key-here-CHANGE-THIS

# Debug mode - MUST be False in production
DEBUG=False

# Allowed hosts - REQUIRED - Comma-separated list of your domain(s)
# Example: ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,api.yourdomain.com
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Database URL - REQUIRED - PostgreSQL connection string
# Railway provides this automatically as DATABASE_URL environment variable
# Format: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://user:password@host:port/database

# CORS allowed origins - REQUIRED - Comma-separated list of frontend URLs
# Example: CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# CSRF trusted origins - REQUIRED - Should match CORS_ALLOWED_ORIGINS
# Example: CSRF_TRUSTED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
CSRF_TRUSTED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# SSL/HTTPS settings
SECURE_SSL_REDIRECT=True
SECURE_HSTS_SECONDS=31536000

# Email configuration (optional, if you need email functionality)
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-email-password
# DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# Logging level (optional, defaults to INFO)
# LOG_LEVEL=INFO
# DJANGO_LOG_LEVEL=INFO

# Environment indicator (optional, used by settings/__init__.py)
# ENVIRONMENT=prod
