# Local Development Environment Variables
# Copy this file to .env.local and fill in your actual values
# DO NOT commit .env.local to version control!

# Django Secret Key - Generate a new one for local development
# You can generate one with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=django-insecure-dev-key-change-me-for-local-development

# Debug mode (True for local development)
DEBUG=True

# Allowed hosts (comma-separated, no spaces after commas)
ALLOWED_HOSTS=localhost,127.0.0.1

# Database URL (optional - defaults to SQLite if not set)
# For PostgreSQL: postgresql://user:password@localhost:5432/karst_db
# For SQLite (default): leave empty or comment out
# DATABASE_URL=sqlite:///db.sqlite3

# CORS allowed origins (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# CSRF trusted origins (comma-separated, should match CORS for local dev)
CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://localhost:3000

# Optional: If you need to test with mobile device on local network
# CSRF_TRUSTED_ORIGINS=http://localhost:5173,http://localhost:3000,http://YOUR_IP:5173
